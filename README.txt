1. Clone project from repository
2. Install python 3.8 using pyenv (pyenv install <version>)
2. Create virtualenv with python 3.8 (virtualenv <name>) and add it as a interpreter in project
3. Install pluggins from requirements.txt
